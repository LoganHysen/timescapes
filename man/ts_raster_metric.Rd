% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{ts_raster_metric}
\alias{ts_raster_metric}
\title{Calculate a binary timescape metric for each raster cell}
\usage{
ts_raster_metric(r, metric, ...)
}
\arguments{
\item{r}{A \code{terra::SpatRaster} whose layers are ordered time steps.}

\item{metric}{One exact metric name returned by \code{\link[=ts_list_metrics]{ts_list_metrics()}}.}

\item{...}{Named metric-specific arguments passed to \code{\link[=ts_metric]{ts_metric()}}.}
}
\value{
A one-layer \code{terra::SpatRaster} containing the selected metric.
}
\description{
Applies one named timescape metric independently to every cell of a
\code{terra::SpatRaster}, treating its layers as ordered time steps. Raster cells
must contain only \code{0}, \code{1}, or \code{NA} through time. A cell containing any \code{NA}
receives an \code{NA} result.
}
\examples{
r <- terra::rast(nrows = 2, ncols = 2, nlyrs = 4)
terra::values(r) <- matrix(
  c(
    1, 1, 0, 1,
    0, 0, 0, 0,
    1, 0, 1, 0,
    1, 1, 1, 1
  ),
  nrow = terra::ncell(r),
  byrow = TRUE
)
ts_raster_metric(r, "n_periods")

}
