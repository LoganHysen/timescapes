## First submission

This is the first CRAN submission of `timescapes`.

## R CMD check results

Checked with `R CMD check --as-cran` on:

- Windows 11 x64
- R 4.6.1

The check produced 0 errors, 0 warnings, and 1 note:

```text
Maintainer: 'Logan Hysen <hysenlog@msu.edu>'

New submission
```

The note is expected for a first CRAN submission. Checks on R-devel, Linux,
and macOS remain to be run before submission.

## Downstream dependencies

There are no downstream dependencies because this is a new package.
